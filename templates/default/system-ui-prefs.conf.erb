#
# $SPLUNK_HOME/etc/system/local/ui-prefs.conf
#
# Generated by Chef templates/default/system-ui-prefs.conf.erb
#

# This will set the default The following ui preferences will default timerange
# picker on the all search pages page from All time to to last 15 minute

<%-if node[:splunk][:ui_prefs][:enable] -%>
[default]
dispatch.earliest_time = <%= node[:splunk][:ui_prefs][:dispatch_etime] -%>

dispatch.latest_time = <%= node[:splunk][:ui_prefs][:dispatch_ltime] -%>

display.page.search.mode = <%= node[:splunk][:ui_prefs][:search_mode] -%>
<%-end -%>
